//cstddef header

#include <cstddef>


size_t obj = 0;


ptrdiff_t ect = 0;
//The result of subtracting two pointers is a library type named ptrdiff_t


difference_type
//A distance is the result of subtracting an iterator by another
//by distance we mean the amount by which we’d have to change one iterator to get the other 
//Both vector and string define difference_type


