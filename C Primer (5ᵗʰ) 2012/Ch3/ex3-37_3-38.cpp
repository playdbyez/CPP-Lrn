//3.37
// pointer *cp points to the first element in the ca[] array and while it increments through the array it prints itself one character per line.


//3.38
// because pointer *cp already traverses through the array without the need for a second declaration.
